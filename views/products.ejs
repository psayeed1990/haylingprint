<div class="page">
    <div id="product-front">


        <% if (typeof doc == 'undefined') { %>
        <p>hi</p>
        <% } %>



        <% if (typeof doc != 'undefined') { %>
            
        <% doc.forEach((product) => { %>

        <div class="single-product">
            <img src="/img/products/<%= product.imageCover %>" width="100%">
            <a href="/products/<%= product.id%>">
                <h3><%= product.name %></h2>
            </a>

            <!-- <a href="/categories/<%= product.category.id %>">
                <p>Category: <%= product.category.name %></p>
            </a> -->
            <p> <%= product.description %></p>
            
            <p>Price: <span class="numbers"><%= product.price %></span></p>
            <p>Stock Left: <span class="numbers"> <%= product.stock %></span></p>
            
            <a href="/products/<%= product.id%>"><button id="add-cart" class="add-cart">Buy Now</button></a>
 
            
        </div>

        <% }) %>


        <% } %>

    </div>
            
</div>